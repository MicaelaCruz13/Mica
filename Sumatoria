// 10) Sumatoria
void sumatoriaNPrimeros() {
    int n, suma = 0;

    printf("Ingrese un número entero N: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        suma += i;
    }

    printf("La suma de los primeros %d números enteros es: %d\n", n, suma);
}

